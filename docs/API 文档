### 🛵 滑板车租赁系统 API 文档

#### 1. 用户模块 (User Controller)
*   **接口前缀**: `/api/users`

| 功能 (Function) | 请求方式 (Method) | 接口路径 (Path) | 请求参数 (Parameters) | 成功响应 (Success Response) |
|---|---|---|---|---|
| **用户注册 (F01)** | `POST` | `/register` | **Body**: `User` 对象, **Query**: `confirmPassword` | `200 OK`, "Registration Successful..." |
| **用户/管理员登录 (F01)** | `POST` | `/login` | **Query**: `username`, `password` | `200 OK`, `User` 对象 (带 `role`) |
| **获取所有用户 (Admin)** | `GET` | `/` | 无 | `200 OK`, `User` 列表 |

---

#### 2. 滑板车模块 (Scooter Controller)
*   **接口前缀**: `/api/scooters`

| 功能 (Function) | 请求方式 (Method) | 接口路径 (Path) | 请求参数 (Parameters) | 成功响应 (Success Response) |
|---|---|---|---|---|
| **新增滑板车 (F16)** | `POST` | `/add` | **Body**: `Scooter` 对象 | `200 OK`, "Scooter added..." |
| **获取所有滑板车** | `GET` | `/` | 无 | `200 OK`, `Scooter` 列表 |
| **获取指定ID的滑板车** | `GET` | `/{id}` | **Path**: `id` | `200 OK`, `Scooter` 对象 |
| **删除滑板车 (Admin)** | `DELETE` | `/{id}` | **Path**: `id` | `200 OK`, "Scooter ... removed." |
| **获取可用车辆坐标 (地图)**| `GET` | `/locations` | 无 | `200 OK`, `ScooterLocationDTO` 列表 |

---

#### 3. 预订模块 (Booking Controller)
*   **接口前缀**: `/api/bookings`

| 功能 (Function) | 请求方式 (Method) | 接口路径 (Path) | 请求参数 (Parameters) | 成功响应 (Success Response) |
|---|---|---|---|---|
| **下单预订 (F05)** | `POST` | `/place` | **Body**: `Booking` 对象 (含 `packageId`) | `200 OK`, `Booking` 对象 (带新ID) |
| **模拟支付 (F06)** | `POST` | `/pay/{bookingId}` | **Path**: `bookingId`, **Query**: `cardNumber`| `200 OK`, "Payment Success..." |
| **延长预订 (F11)** | `POST` | `/extend/{bookingId}`| **Path**: `bookingId`, **Query**: `extraCost`| `200 OK`, "Booking extended..." |
| **结束行程 (F10)** | `POST` | `/end/{bookingId}` | **Path**: `bookingId` | `200 OK`, "Trip ended..." |
| **取消预订 (F12)** | `POST` | `/cancel/{bookingId}`| **Path**: `bookingId` | `200 OK`, "Booking canceled..." |
| **管理员代下单 (F09)** | `POST` | `/admin/place` | **Body**: `Booking` 对象 (含 `guestName`)| `200 OK`, `Booking` 对象 |
| **查看用户订单 (F08)** | `GET` | `/user/{userId}` | **Path**: `userId` | `200 OK`, `Booking` 列表 |
| **周收入统计 (F19)** | `GET` | `/admin/revenue` | 无 | `200 OK`, `RevenueReport` 列表 |
| **日收入统计 (F20)** | `GET` | `/admin/revenue/daily`| 无 | `200 OK`, `DailyRevenueReport` 列表 |

---
